package com.code.interview.bit;

public class MakingStringPallindromic {
	
	public int solve(String A) {
		int len = A.length();
		if(len<=1) {
			return 0;
		}
		int mid = len / 2;
		if(len%2==1) {
			for(int i=mid;i>=0;i--) {
				
			}
		} else {
			
		}
		
		
		return 0;
		
    }

}
